apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: external-secrets
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"

spec:
  goTemplate: true

  generators:
    - clusters:
        selector:
          matchLabels:
            cluster-type: workload

  template:
    metadata:
      name: '{{nameNormalized}}-external-secrets'

    spec:
      project: default

      sources:
        # Source 1: External Secrets Helm chart
        - repoURL: https://charts.external-secrets.io
          chart: external-secrets
          targetRevision: 0.9.0
          helm:
            values: |
              serviceAccount:
                create: true
                name: external-secrets

        # Source 2: Our GitOps configuration
        - repoURL: https://github.com/Shashankcj/platform.git
          targetRevision: main
          path: platform/external-secrets
          helm:
            values: |
              vault:
                authMount: '{{ index .metadata.labels "vault-auth-mount" }}'

      destination:
        server: '{{server}}'
        namespace: external-secrets

      syncPolicy:
        automated:
          prune: true
          selfHeal: true

        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true